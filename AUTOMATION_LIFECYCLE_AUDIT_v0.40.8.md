# NEUL v0.40.8 自動活動生命週期稽核

日期：2026-09-20（Asia/Taipei）

## 自動來源更新

- `/api/events` 每次重新驗證時同時整併 14 類台灣售票來源、Live Nation Taiwan、藝人官方與場館官方來源。
- API 使用 1 小時 CDN cache；開啟中的網頁每 1 小時重新讀取 `/api/events`。
- 網頁重新回到前景時，如果資料已超過 1 小時，立即重新驗證。
- `/api/official` 的前端重驗週期由 6 小時縮短為 1 小時，用於官方頁日期／售票日／場館等修正。
- 上游暫時失敗時保留已核對資料，不因單一來源連線失敗刪除活動。
- Vercel 排程 `/api/refresh` 保留每日 warm-up；即使沒有訪客，仍保留每日排程作為保底同步。

## 售票與演出倒數狀態機

1. 已取得下一個官方開賣時間，而且尚未到達：`搶票倒數`。
2. 開賣當日：`今日開賣`，保留到台灣時間當日 23:59。
3. 售票日結束後、演出尚未開始：自動切換為 `距離演出` 倒數。
4. 演出已開始且未超過有效結束時間：`演出進行中`。
5. 最後場次結束：`已結束`，自動進入 Archive。

多日活動優先使用 `sessions` 的最後一場判斷結束；只有日期沒有時間時，保留至該日 23:59，避免當天凌晨誤歸檔。

## Archive / 3D 選單

- 一般 Upcoming 清單不顯示已結束活動。
- Archive 只顯示已結束活動。
- 3D 活動配置選單會移除已結束活動的 event layout。
- 如果使用者當下選中的活動剛結束，3D 會自動切到該場館下一個未結束活動；若沒有則回到一般場館 3D。
- 每次 API 資料更新後立即重建 3D 選單，無須等待下一個分鐘輪詢。
- 未來活動依日期排序，因此新活動抓到後會自動進入正確位置。

## Featured

- Featured 候選只取台灣、尚未結束的活動。
- 最多 10 場。
- 每 10 秒自動切換下一場。
- 使用者按上一個／下一個後，10 秒自動輪播重新起算。
- 頁面在背景時暫停輪播，回到前景後繼續，避免不必要工作。

## QA 結果

- `npm run check`：PASS
- 75 筆 seed fallback 活動
- 24 個場館模型
- 14 類售票平台＋藝人／場館官方來源
- 7 個 Serverless Functions（低於 12 Functions 門檻）
- 售票狀態 fixture：開賣前 → 開賣日 → 售票日後，PASS
- 自動解析短格式售票日期 `10/14 12:00`：PASS
- Archive / 3D expired layout cleanup：PASS
- Featured 10 秒輪播：PASS
- `index.html` 與 `styles.css` 相較 v0.40.7 SHA-256 完全一致，網站設計未變。

## 正確性界線

NEUL 能自動驗證「已配置來源公開提供」的演唱會資料，但不能宣稱任何公開來源集合可 100% 覆蓋全台所有演出。系統會顯示來源健康狀態並保留 fallback，避免把來源暫時失敗誤判為活動取消。
