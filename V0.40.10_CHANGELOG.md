# NEUL v0.40.10

- 官方座位圖 resolver 升級為 recursive-v2。
- 加入合併後 `sourceRefs` 全來源座位圖候選。
- 支援 JSON/script escaped seat-map URL 與 relative asset path。
- 多來源逐一 fallback，單一來源失敗不再中止。
- 可從官方索引／入口頁繼續追蹤 detail page。
- 成功解析的 resolved URL / hash 保留於校正快取，避免每小時事件同步洗掉結果。
- resolver 失敗保留重試狀態，不再直接等同官方未公布。
- 官方監控改為缺圖優先＋每小時輪替，避免活動數成長後固定尾端永遠不驗證。
- 未更動原網站 `index.html` / `styles.css`。
