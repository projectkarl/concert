# NEUL v0.40.12 Changelog

## Activity calendar
- Keep the monthly calendar and the original complete filtered list at the same time.
- Add a selected-day agenda between the calendar and full list.
- Fill leading/trailing blank calendar cells so the month presents as standard week rows.

## 3D official reference
- Put the official seat-map reference beside the 3D seat view on desktop; stack on mobile.
- Keep the panel visible for event layouts even while the official map is pending.
- Reuse the seat-map resolver cached resolved URL so a previously discovered official image remains visible after refresh.
- Proxy first, then try the resolved image directly if the proxy cannot display it.

## Featured
- Preserve the 10-event Featured pool.
- Replace the passive interval with a restartable 10-second timer.
- Add visible AUTO countdown/progress.
- Restart autoplay after manual navigation and after returning to the tab.

## Compatibility
- No change to the underlying coverage auditor, archive lifecycle, source recovery, future-event custom 3D generator, WebGL/Canvas renderer or PWA persistence.
