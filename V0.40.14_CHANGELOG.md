# NEUL v0.40.14 — Activity Explorer + 3D Recovery

## 活動查詢體驗
- 移除「保留原本清單，可直接往下瀏覽全部活動」說明文字。
- 完整活動清單改為查詢頁主要內容，桌機左側直接顯示全部符合篩選的活動；右側保留月曆與當日 agenda。
- 手機版改為完整清單優先、月曆接續在下方，不再需要先滑過整個月曆才能看到全部活動。
- 搜尋改成多關鍵字 token matching；藝人、活動名稱、場館、城市、標籤可交叉查詢。
- 月份、開始／結束日期、地區與快速 30/90 天篩選維持。

## 3D 修復
- 修正 Canvas 3D fallback 中 `isLight` 在宣告前被使用的 runtime error；WebGL 無法使用時不再直接空白。
- Safari / WebKit 預設使用穩定互動 Canvas 3D 路徑，避免 WebGL context/shader 差異讓三個 3D canvas 同時失效。
- 非 Safari 仍優先 WebGL；若 WebGL runtime 發生錯誤，會記錄單次安全模式並重新初始化 Canvas 3D，而非卡在 loading/blank。
- Canvas 3D 仍為可旋轉、縮放、座位視角計算的動態 3D 投影，不是靜態圖片。
- 補上台北南港展覽館一館場館模型與名稱解析，修復 Jason Mraz 活動沒有可用 3D layout 的問題。

## QA
- `npm run check`: PASS
- Future-event automatic custom 3D pipeline: PASS
- 75/75 current fallback events runtime 3D audit: PASS
- 75/75 unique event-specific 3D audit: PASS
- HTML required IDs / duplicate ID audit: PASS
