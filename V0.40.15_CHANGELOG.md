# NEUL v0.40.15

## 3D layout rollback to v0.40.11 composition
- Restored the v0.40.11 single-column 3D preview composition.
- Removed the v0.40.12+ side-by-side `seat-preview-reference-grid` that could compress the preview and cause responsive overflow.
- Kept the newer 3D runtime recovery, Safari-safe Canvas fallback, official seat-map resolver, auto custom-3D pipeline, archive lifecycle, coverage auditor and automatic updates.
- IVE remains only in the activity-layout dropdown; the old standalone demo card is not restored.

## Official seat-map placement
- Official position / seat-layout reference is now placed below the seat-view simulation and its explanatory copy.
- It no longer changes the preview canvas width or sits beside the 3D preview.
- Existing pending / auto-backfill messaging and official-source link remain supported.

## QA
- Future-event automatic custom 3D pipeline: PASS.
- Runtime 3D audit: 75/75 PASS.
- Test now explicitly fails if the side-by-side 3D grid returns or if the official reference is not below the preview explanation.
