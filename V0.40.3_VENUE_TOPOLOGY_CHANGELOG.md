# NEUL v0.40.3 — Venue Topology Guard

## 2026-09-21

### 修正
- 彭佳慧台北／高雄場共用 KHAM 同一 PRODUCT_ID 時，座位圖解析改為帶入 `venue`、`venueModelId`、`seatMapVariantHint`，不再直接採第一張票區圖。
- 共用售票頁若無法依場館提示消歧義，resolver 會拒絕套圖，避免把其他城市／場館圖套入本場。
- TICC 大會堂移除錯誤的 arena 式多層環形模型；2MF／3F／4F／5F／6F 改為連續斜坡觀眾席的分帶模型。
- TICC 拓樸規則明確禁止一般 `1F` 觀眾席自動生成。
- 修正未知 `venueModelId` / `venueLayoutId` 靜默 fallback 到臺北大巨蛋的風險。

### 自動 3D 防錯
- 新增 `VENUE_GEOMETRY_POLICIES` 與 Venue Topology Guard。
- 固定看台／固定樓層維持場館模型 authoritative；OCR/Vision 不可整場取代。
- 只允許在可變區新增 event-specific 票區：例如小巨蛋 B1、北流 1F、Zepp 1F、各 arena floor。
- 固定劇院／會議廳可禁止座位圖覆寫固定舞台；TICC 已啟用。
- 未校正場館預設停止產生 generic 3D；只有明確連結官方座位圖且 `allowProvisional3D=true` 才可建立暫定模型。

### QA
- 101 場逐筆 venue/model/layout audit。
- 71 場使用 13 個已校正場館模型生成 topology-safe event 3D。
- 30 場戶外／臨時／未校正場館停止生成猜測式 3D。
- 新增共享售票頁台北／高雄雙圖 resolver fixture。
- 新增 TICC continuous-rake、無 1F audience tier、unknown venue no-fallback regression checks。
